/**
 * Central export for the file store.
 * Import from here: import { useFileStore } from '../store'
 */
export { useFileStore } from './useFileStore'
export { PERSIST_STORAGE_KEY } from './persistConfig'
export { defaultFileTree, DEFAULT_ACTIVE_FILE_ID } from './defaultProject'
