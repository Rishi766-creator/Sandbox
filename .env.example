# Optional: subpath deploy (default "/")
# VITE_BASE_PATH=/

# Optional: override esm.sh CDN origin for preview packages
# VITE_ESM_SH_ORIGIN=https://esm.sh

# Optional: Monaco editor CDN version (match monaco-editor in package-lock)
# VITE_MONACO_VERSION=0.55.1
